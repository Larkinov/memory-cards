import { Typography } from "@mui/material";
import React from "react";

const NotFoundPage: React.FC = () => {
  return (
    <>
      <Typography variant="h3" p={"40px"}>
        Данная страница не существует!
      </Typography>
    </>
  );
};

export default NotFoundPage;
